# Журнал обновлений бота (Changelog)

*Changelog ведётся с 19.03.2026. Записи отсортированы от новых к старым.*

---

## 🛠 V1.11.9h — Кнопка «📊 Не в чате» оживлена (25.04.2026)

### 🐛 БАГ
▪️ Кнопка «📊 Не в чате» в Панели Владельца **не реагировала** — пользователь видел крутящийся loader, который потом просто исчезал
▪️ Три причины:
   1. Не было `await query.answer()` — Telegram требует ответа в течение 15 сек
   2. Имена/ники юзеров **не HTML-экранировались** — спецсимволы (`<`, `&`) в именах ломали `parse_mode='HTML'`
   3. Если `edit_message_text` падал — ошибка логировалась молча, юзеру ничего не отправлялось

### ✅ ИСПРАВЛЕНО (V1.11.9h)
▪️ Добавлен `query.answer()` сразу при входе в функцию
▪️ `html.escape()` для имени и ника каждого юзера
▪️ Fallback: если `edit_message_text` падает — отправляется новое сообщение
▪️ В крайнем случае показывается alert с текстом ошибки
▪️ Файл: `handlers/owner_handlers.py`

---

## 🧹 V1.11.9f — Демонтаж aiogram-бота (25.04.2026)

### 🎯 ЗАЧЕМ
▪️ Aiogram-бот после интеграции с PTB больше не запускается (`pulsbot.service` — единственный)
▪️ Его БД (`registration_system/pulse_bot.db`) заморожена с 27.02.2026
▪️ Все 5 юзеров с заполненной анкетой из этой БД **уже есть** в основной PTB-БД
▪️ Директория `registration_system/` весит сотни МБ из-за `node_modules`, `venv`, `.venv`

### ✅ КОД (V1.11.9f)
▪️ Убран fallback `from registration_system.database import get_user as _get_reg_user` из:
   - `handlers/command_handler.py:start_command`
   - `handlers/registration_conversation.py:start_reg`
▪️ `handlers/owner_handlers.py:show_statistics_not_in_chat` (БЗА/НПС панель) переключён на основную PTB-БД (`pulse_bot.db` в корне) вместо устаревшей aiogram-БД — статистика теперь актуальная
▪️ `run_auto.py`: убран `registration_system` из `IGNORE_DIRS`
▪️ `.github/workflows/deploy.yml`: убран бэкап/восстановление `registration_system/pulse_bot.db`

### 🗑 К УДАЛЕНИЮ ОТДЕЛЬНЫМ КОММИТОМ
- Директория `registration_system/` целиком (~сотни МБ)
- `test_face.py` (тестировал дубль face_detector'а)
- `script/cleanup_reg.py`, `script/migrate_db.py` (потеряли смысл)

---

## 🔄 V1.11.9e — Возвращающиеся юзеры не получают анкету повторно (25.04.2026)

### 🐛 БАГ
▪️ После выхода из чата юзеру при `/register` (или клике «📝 Подать заявку») бот **снова прогонял через всю анкету**, как нового
▪️ Причина: `start_reg` в `registration_conversation.py` не проверял что у юзера уже есть `q_name`/одобренная заявка
▪️ `restart_registration` callback бездумно стирал статус, даже для возвращающихся

### ✅ ИСПРАВЛЕНО (V1.11.9e)
▪️ В `start_reg` добавлена цепочка проверок «возвращающийся» ДО показа анкеты:
   1. `q_name` в `db_friend.users`
   2. `q_name` в `registration_system.database` (старый aiogram-бот)
   3. Одобренная заявка в `db_friend.applications`
▪️ Если хотя бы одна проверка True → статус в чате (`getChatMember`); если не `kicked` → генерируется одноразовая invite-ссылка
▪️ В `restart_registration` callback (оба файла: `callback/callback_router.py` и `utils/callback_handler.py`) добавлена защита: возвращающимся (с `q_name`) данные не стираются
▪️ Файлы: `handlers/registration_conversation.py`, `handlers/callback/callback_router.py`, `handlers/utils/callback_handler.py`

---

## 📊 V1.11.9d — Упоминания @ считаются корректно (25.04.2026)

### 🔧 ИСПРАВЛЕНО: mentions_received начислялся отправителю (V1.11.9d)
▪️ Раньше: если **A** пишет «@B @C», поле `mentions_received` увеличивалось на 2 у **A** (отправителя)
▪️ B и C не получали ничего, хотя именно их упомянули
▪️ Это ломало:
   - 🏆 Индекс активности (вес 0.15)
   - 💱 Курс Пульса (тот же вес в delta)
   - 📊 Excel-выгрузки (колонка КУПП)
   - 🥇 ТОП-5 активистов

### ✅ ТЕПЕРЬ
▪️ Парсятся **оба типа** упоминаний:
   - `mention` (text `@username`) — username ищется в `users` (case-insensitive)
   - `text_mention` (тап по имени) — `entity.user.id` берётся напрямую
▪️ `mentions_received` начисляется **каждому упомянутому** пользователю (event_id уникальный — `mention_recv_{user}_{msg}`)
▪️ У отправителя `mentions_received` **не увеличивается** (он не получил упоминание, а сделал)
▪️ Self-mention и упоминание ботов игнорируются
▪️ Дельта курса: один раз за сообщение если есть хотя бы одно упоминание
▪️ Файл: `handlers/message_handler.py`

⚠️ **Исторические данные за прошлые периоды содержат завышенные значения у активных писателей и заниженные у тех, кого часто упоминали** — это последствия старого бага.

---

## 🤝 V1.11.9c — Реферальная система: оба пути починены (25.04.2026)

### 🔗 ПУТЬ 1: РУЧНОЙ @username (V1.11.9c)
▪️ Раньше юзер вводил `@nersys` → строка сохранялась в `referred_by` (INTEGER колонка) → битая ссылка
▪️ Теперь на шаге REF_CODE: `@`/пробелы стрипаются, ник ищется в `db_friend` и `main_db` (case-insensitive)
▪️ Если **не найден** — показывается ошибка с возможностью ввести заново или пропустить
▪️ Если **самого себя** — отказ
▪️ В БД сохраняется **integer tg_id** реферера, не строка
▪️ Файл: `handlers/registration_conversation.py`

### 🔗 ПУТЬ 2: DEEP-LINK ref1_xxx (V1.11.9c)
▪️ Раньше: новый юзер кликает реф-ссылку → бот показывает «📝 Подать заявку» и **`return`** ДО обработки токена → токен терялся
▪️ Теперь токен обрабатывается **в самом начале** `start_command`:
   - Поиск referrer_id по токену
   - Создание записи в `db_friend` (если ещё нет)
   - Сразу пишется `referred_by=referrer_id` (integer)
   - Токен сохраняется в `context.user_data` как fallback
▪️ Deep-link имеет **приоритет** над ручным вводом @username
▪️ После завершения регистрации токен помечается использованным (`use_referral_link`)
▪️ Файлы: `handlers/command_handler.py`, `handlers/registration_conversation.py`

### ✅ ОДОБРЕНИЕ: integer-валидация + pending-транзакция (V1.11.9c)
▪️ При одобрении заявки `referred_by` принудительно приводится к `int()` с защитой от `ValueError`
▪️ Защита от self-referral на уровне одобрения
▪️ Создаётся запись в `referral_transactions` со статусом `pending` (через `process_referral`)
▪️ Файл: `handlers/admin_moderation.py`

### 🎁 НАЧИСЛЕНИЕ: confirm_referral при вступлении (V1.11.9c)
▪️ Когда юзер реально вступил в чат — `confirm_referral(user_id)` переводит транзакцию из `pending` в `credited`
▪️ Счётчик рефералов и заработок реферера увеличиваются автоматически
▪️ Файл: `handlers/messages/events_logic.py`

### 📊 EXCEL-ЭКСПОРТ: user_joins пишется при вступлении (V1.11.9c)
▪️ Раньше `record_user_join()` вызывался только в `_start_command`, до которого новые юзеры не доходили → лист «👥 ВСТУПЛЕНИЯ В ЧАТ» в Excel был пустым
▪️ Теперь при реальном входе в чат (`handle_user_returned`) автоматически определяется `join_method`:
   - `referral_link` — если `referred_by` заполнен
   - `chat_invite_link` — если вошёл по `invite_link`
   - `direct_invite` — если был добавлен другим пользователем
   - `chat_member_joined` — самостоятельное вступление
▪️ Запись содержит `referrer_id` (integer) для корректного JOIN в экспорте
▪️ Файл: `handlers/messages/events_logic.py`

---

## 🔐 V1.11.9b — Безопасность invite-ссылок (25.04.2026)

### 🛡 УСИЛЕНА ПРОВЕРКА ChatJoinRequest (V1.11.9b)
▪️ Раньше при входе по invite-ссылке проверялось только наличие записи в БД и `is_banned=False`
▪️ Теперь декларативно отклоняем тех, кто:
   - Нет в базе регистраций
   - В чёрном списке (`is_blacklisted`)
   - Забанен (`is_banned`)
   - Не имеет заполненной анкеты (`q_name` пустой)
   - Имеет недопустимый статус: `pending`, `new`, `in_work`, `blocked`, `rejected`, `banned`
▪️ После одобрения сразу синхронизируем основную БД — `is_left=0`, не дожидаясь первого сообщения
▪️ Файл: `bot.py`

---

## 🐛 V1.11.9 — Трекер багов, досье, уведомления о новых участниках (25.04.2026)

### 🐛 ТРЕКЕР БАГОВ — ПОЛНЫЙ ЗАПУСК (V1.11.9)
▪️ Устранена корневая причина: `BUG_THREAD_BOT` и `BUG_THREAD_SITE` отсутствовали в `config/__init__.py` → бот падал с ImportError при каждом сообщении в админ-чате
▪️ Сообщения в форум-топиках всегда имеют `reply_to_message` (ссылка на корень топика) — исправлена проверка: теперь отличаем «обычное сообщение в треде» от «ответа на другое сообщение»
▪️ `reply_text` заменён на `bot.send_message()` с явным `chat_id` и `message_thread_id` — надёжная отправка в форум-ветке
▪️ Исходное сообщение пользователя **удаляется** после создания карточки — нет дублей
▪️ Кнопка **✏️ Редактировать** теперь спрашивает комментарий прямо в треде через `ForceReply` (раньше уводила в ЛС)
▪️ Доступ к трекеру: **Витя** (OWNER_ID) и **Илья** (DEVELOPER_ID)
▪️ Файлы: `handlers/bug_tracker_handlers.py`, `handlers/message_handler.py`, `config/__init__.py`

### 📋 ДОСЬЕ — РУЧНАЯ ПУБЛИКАЦИЯ /resend_dossier (V1.11.9)
▪️ Новая команда `/resend_dossier <user_id>` — принудительно публикует досье участника из БД в ветку Досье
▪️ Показывает диагностику (chat_id, thread_id) перед отправкой; при ошибке сообщает точный текст
▪️ Доступна Вите и Илье (@Nersys)
▪️ Описание добавлено в раздел FAQ → Команды Владельца
▪️ Файл: `handlers/command_handler.py`, `handlers/commands/system_commands.py`

### 👤 УВЕДОМЛЕНИЯ О НОВЫХ УЧАСТНИКАХ (V1.11.9)
▪️ Исправлен баг: `handle_user_returned` выходил раньше `log_join` если пользователь ещё не был в основной БД (новый участник, не возвращающийся)
▪️ Теперь журнал фиксирует **всех** новых вступивших, а не только возвращающихся
▪️ Файл: `handlers/messages/events_logic.py`

### 🔧 ТЕХНИЧЕСКИЕ ФИКСЫ (V1.11.9)
▪️ **emojis.py**: удалён дублирующий `ICON_GREEN_CIRCLE` с невалидным emoji-id > max uint64 — ломал HTML-парсинг всех досье; исправлен `ICON_RED_EXCLAMATION_MARK` (тот же тип ошибки)
▪️ **anketa_edit_handlers.py**: маркер присутствия `<!-- presence -->` заменён на невидимый символ U+200C — Telegram не поддерживает HTML-комментарии в `parse_mode=HTML`
▪️ **config/__init__.py**: добавлены `DEVELOPER_ID`, `BUG_THREAD_BOT`, `BUG_THREAD_SITE`

---

## 🛡 V1.11.8d — Фильтр кнопок в статистике + убраны ЛС-оповещения (23.04.2026)

### 🔕 УБРАНЫ ЛС-ОПОВЕЩЕНИЯ О ЗАМОРОЗКЕ (V1.11.8d)
▪️ Администраторы и владелец **больше не получают личные сообщения** о замороженных пульсах пользователей
▪️ Информация о заморозке доступна **только в Журнале событий** — никакого спама в ЛС
▪️ Файл: `handlers/messages/events_logic.py`

### 🚫 КНОПКИ МЕНЮ НЕ СЧИТАЮТСЯ СООБЩЕНИЯМИ (V1.11.8d)
▪️ Нажатия reply-кнопок («📊 Курс», «💰 Баланс», «👤 Профиль» и др.) **не засчитываются** как сообщения пользователя в статистику
▪️ Добавлена проверка `is_btn_press` в блок `should_ignore`
▪️ Файл: `handlers/message_handler.py`

---

## 🔐 V1.11.8c — Система дедупликации stat_events_log (22.04.2026)

### 🆕 ДЕДУПЛИКАЦИЯ НА УРОВНЕ БД (V1.11.8c)
▪️ Новая таблица `stat_events_log` — каждое событие статистики имеет уникальный `event_id`
▪️ Форматы ID: `msg_{uid}_{msg_id}`, `react_given_{uid}_{msg_id}_{date}`, `react_recv_{author}_{msg_id}_{date}`, `reply_recv_{uid}_{msg_id}`
▪️ `INSERT OR IGNORE` — повторное событие молча игнорируется, данные не дублируются
▪️ Таблица очищается раз в 3 дня (события старше 3 дней удаляются автоматически); исторические данные в `user_stats` остаются навсегда
▪️ Файлы: `database/db_stats.py`, `database/db_migrations.py`, `database/db_manager.py`, `bot.py`

---

## 🔧 V1.11.8b — Исправлен двойной счёт реакций и реплаев (22.04.2026)

▪️ Устранён баг: `reactions_given` и `replies_received` считались дважды из-за двух независимых путей вызова `update_user_activity`
▪️ Удалён дублирующий вызов (строка-«сирота»), оставлен единственный авторитетный
▪️ **Данные за прошлые периоды могут быть завышены** — это результат старого бага, не новой ошибки
▪️ Файл: `handlers/messages/events_logic.py`

---

## 📊 V1.11.8 — Статистика: точные данные и исправленный Excel (21.04.2026)

### 📅 ИСПРАВЛЕНЫ ДАТЫ «ЗА ВЧЕРА» (V1.11.8)
▪️ Период «За вчера» в тексте статистики **и** в файле Excel теперь не захватывает сегодняшний день
▪️ Файлы: `handlers/Stats/stats_controller/callback.py`, `handlers/Stats/stats_exporters.py`

### 💾 ИСТОЧНИК ДАННЫХ — user_stats (V1.11.8)
▪️ «Добыто» (майнинг) теперь берётся из `user_stats` — данные чище, не дублируются с транзакциями
▪️ Excel: все запросы переведены с устаревшей `chat_stats` на `user_stats`
▪️ Защита от деления на ноль — добавлена проверка `NULLIF` для периодов без сообщений

### 📋 EXCEL — РАЗДЕЛЕНИЕ ПО АДМИНИСТРАТОРАМ (V1.11.8а / V1.11.8а1)
▪️ Таблица Excel корректно разделяет данные по администраторам
▪️ Нули `.00` убраны из числовых ячеек — числа отображаются как целые
▪️ Бот не считает реплаи на собственные команды как ответы пользователям
▪️ Файлы: `handlers/Stats/stats_exporters.py`, `handlers/message_handler.py`

---

## 📈 V1.11.7 — Переработка системы статистики (16.04.2026)

### 🗃 ЕДИНЫЙ ИСТОЧНИК ДАННЫХ (V1.11.7)
▪️ Все упоминания `chat_stats` заменены на `user_stats` — единый источник правды
▪️ Если сообщение записалось в БД, оно **гарантированно** отобразится во всех пунктах статистики
▪️ СДС (средняя длина сообщения) теперь считается динамически: сумма символов / сумма сообщений

### ⚡ RACE CONDITION — УСТРАНЁН (V1.11.7а1)
▪️ Запись в `user_stats` стала атомарной через `ON CONFLICT(user_id, date) DO UPDATE SET col = col + excluded.col`
▪️ Системный мусор отсечён фильтром `~filters.StatusUpdate.ALL`

### 🕐 ПОЧАСОВАЯ СТАТИСТИКА — ПОЧИНЕНА (V1.11.7а2)
▪️ Одна строка на пользователя в час вместо многократного дублирования — ТОП-5% больше не показывает 140%+
▪️ Реакции: засчитывается только факт изменения, не каждый клик — нельзя «нафармить» индекс активности

### 🏆 АМБАСАДОР ЛЮБВИ (V1.11.7а3)
▪️ Награда «Амбасадор Любви» снижена с **20 до 10 пульсов**

---

## 🔕 V1.11.6 — Отключены ЛС-оповещения, обновлена логика ББС (12.04.2026)

### 🔕 УДАЛЕНЫ ЛС-ОПОВЕЩЕНИЯ О ВХОДАХ/ВЫХОДАХ (V1.11.6а)
▪️ Администраторы и владелец **больше не получают** дублирующиеся личные сообщения о входах и выходах участников
▪️ Вся информация — только в **Журнале событий**
▪️ Файл: `handlers/messages/events_logic.py`

### 🔧 ВОССТАНОВЛЕНИЕ АНКЕТ ББС (V1.11.6)
▪️ Обновлена логика восстановления анкет ББС — заявки восстанавливаются корректно после обрыва соединения
▪️ Файл: `handlers/bbs_handlers.py`

---

## ✍️ V1.11.5 — Обновлена подпись бота (08.04.2026)

▪️ Подпись в пресс-релизах и гороскопе изменена: теперь **«© Сообщество Pulse 2026»**
▪️ Файлы: `handlers/press_release.py`, `handlers/horoscope.py`

---

## 🔧 V1.11.4 — Восстановление функций ББС и НьюзON (02.04.2026)

▪️ Починка восстановления анкет ББС — сломалось после апдейта от 01.04.2026
▪️ Кнопка «🎁 Компенсация BBS» возвращена в меню восстановления веток
▪️ Восстановлены ранее удалённые новости из ветки НьюзON
▪️ Расширенный FAQ для владельца — добавлены новые разделы по управлению ботом
▪️ Файлы: `handlers/bbs_handlers.py`, `handlers/news_handlers.py`, `handlers/FAQ/owner_faq.py`

---

## 🚀 V1.10.1 — Баннеры-онбординг, фиксы активностей, донаты (10.04.2026)

### 🎨 БАННЕРЫ-ОНБОРДИНГ (V1.10.1)
▪️ При **первом** входе в раздел активности (Лотерея / Бинго / Донаты / Подарок Месяца) пользователю показывается **полноэкранный рекламный баннер** с описанием функции
▪️ Под баннером кнопка **«✅ Участвовать!»** — после нажатия пользователь попадает в меню, баннер больше не показывается
▪️ File_id кешируется при первой отправке — экономия трафика для последующих пользователей
▪️ Новая таблица: `user_banners_seen` (user_id, banner, seen_at)
▪️ Картинки: `assets/banners/` (lottery, bingo, donate, gift)
▪️ Файлы: `handlers/banner_utils.py` (новый), `handlers/callback/activity_callbacks.py`

### 🔧 ФИКС РОУТИНГА АКТИВНОСТЕЙ (V1.10.0)
▪️ **Бинго** — `activity_callbacks.py` вызывал несуществующий `handle_callback()`. Исправлено: отдельный роутинг `bingo_*` → `handle_bingo_callback`, `bcard_*` → `handle_card_callback`, `bbingo_*` → `handle_bingo_claim`
▪️ **Лотерея** — неправильные имена методов и порядок аргументов: `buy_ticket` → `handle_buy_ticket`, `show_my_tickets` → `handle_my_tickets`, `handle_admin_callback` → `handle_lott_callback` / `handle_lottery_callback`
▪️ **Подарок Месяца** — несуществующий `handle_callback()`. Добавлен единый роутер `handle_callback` в `GiftHandler`; исправлен баг в `handle_monthly_gift_toggle` (передавался `query` вместо `context`)
▪️ **Донат** — исправлен пропущенный аргумент `data` в `donate_user_custom`
▪️ Файлы: `handlers/callback/activity_callbacks.py`, `handlers/gift_handlers.py`

### 🗑 УДАЛЁН РЕАКТОР ИЗ ДОНАТОВ (V1.10.0)
▪️ Удалены функции: `donate_to_reactor_start`, `donate_reactor_custom`, `donate_reactor_amount`, `donate_reactor_confirm`
▪️ Убрана кнопка «🔋 В Реактор» из меню донатов
▪️ Очищена история донатов от упоминаний реактора (SQL-запросы, отображение)
▪️ Файл: `handlers/donate_handlers.py`

---

## 🚀 V1.9.7 — Триггеры++, редактирование анкет, статистика (10.04.2026)

### ✏️ РЕДАКТИРОВАНИЕ АНКЕТ — ФИКСЫ (V1.9.7b1–b8)
▪️ **Медиа в ADMIN_CHAT** — фото-вложения при редактировании анкеты игнорировались (строка `return` перехватывала раньше FSM). Добавлена проверка `anketa_edit` перед `return` в `message_handler.py`
▪️ **Формат досье** — после редактирования менялся формат (использовался `_build_dossier_text` вместо оригинала). Введён `base_text` — сохраняется при создании досье и используется при каждом rebuild
▪️ **Дублирование сообщений** — ошибка «Message is not modified» проваливалась в `send_message`, создавая копии. Добавлен catch с `return`
▪️ **Кнопка ✏️ одноразовая** — `action='done'` вызывал `delete_message()` на досье (меню = досье in-place). Заменено на `_rebuild_and_update`
▪️ **Меню в неправильном треде** — fallback `send_message` не указывал `message_thread_id`
▪️ **Автоудаление подтверждений** — «✅ Фото обновлено» / «✅ Примечание сохранено» теперь исчезают через 4 сек (`asyncio.create_task`)
▪️ **Перепубликация при удалении фото** — при `clrphoto` старое фото-сообщение не удалялось перед отправкой нового текстового
▪️ **ForceReply для примечаний** — вместо редактирования досье в промпт, теперь отправляется отдельное сообщение с `ForceReply` → Telegram автоматически открывает панель ввода
▪️ **Кнопка «📷 Медиа ▾»** — раскрывается в две: «📸 Заменить фото» и «🗑 Убрать фото» (вторая видна только при наличии кастомного фото)
▪️ **Полное досье в меню** — меню редактирования теперь показывает полный текст досье (через `base_text`) вместо обрубка
▪️ Файлы: `handlers/anketa_edit_handlers.py`, `handlers/admin_moderation.py`, `handlers/message_handler.py`

### ✏️ РЕДАКТИРОВАНИЕ АНКЕТ В ВЕТКЕ «АНКЕТЫ» (V1.9.7b)
▪️ К каждому досье в ветке «Анкеты» добавлена кнопка **✏️ Редактировать** рядом с «✉️ Написать в ЛС»
▪️ Возможности редактирования:
  — **📷 Изменить фото** — заменить фото из Telegram-профиля на кастомное (отправить фото в ЛС боту)
  — **📝 Добавить / изменить примечание** — текстовая заметка к анкете (отображается в конце досье)
  — **🗑 Убрать кастомное фото / примечание** — сброс к оригинальному состоянию
▪️ Редактирование **бесконечное** (кнопка ✏️ остаётся всегда)
▪️ При изменении бот **редактирует существующее сообщение** в ветке; если msg_id не сохранён (старые анкеты) — отправляет обновлённую версию новым сообщением
▪️ Работает для всех новых анкет автоматически; для старых — через кнопку в панели
▪️ Новая таблица: `anketa_edits` (хранит кастомное фото, примечание, msg_id досье)
▪️ Файлы: `handlers/anketa_edit_handlers.py` (новый), `handlers/admin_moderation.py`, `handlers/callback/callback_router.py`, `handlers/messages/admin_logic.py`

### ⚡ ТРИГГЕРЫ — ПРЕВЬЮ ССЫЛОК И РЕПЛАЙ (V1.9.7a)
▪️ В настройке действия «💬 Сообщение в чат» добавлены два тумблера:
  — **🔗 Превью ссылок** (✅/❌) — включить/выключить предпросмотр ссылок в ответе бота (по умолчанию включено)
  — **↩️ Реплай** (✅/❌) — бот отвечает с реплаем на сообщение пользователя, которое сработало триггер (по умолчанию выключено)
▪️ Также добавлен тумблер превью для «✉️ Сообщение в ЛС»
▪️ Настройки сохраняются в `action_configs.msg_chat.link_preview` и `action_configs.msg_chat.reply_to_user`
▪️ Файл: `handlers/triggers_handlers.py`

### 🧹 АВТО-УДАЛЕНИЕ ОТВЕТОВ БОТА (V1.9.7a)
▪️ При срабатывании триггер-слов **«курс»**, **«активист»**, **«активисты»**, **«богач»**, **«богачи»** — сообщение пользователя удаляется сразу, ответ бота — **через 60 секунд**
▪️ Аналогично работает кнопка «📊 Курс» в reply-клавиатуре чата
▪️ Файл: `handlers/message_handler.py` (хелпер `_schedule_auto_delete`)

### 📊 СТАТИСТИКА — ИСПРАВЛЕНИЕ «ДОБЫТО ПУЛЬСОВ» (V1.9.7d)
▪️ В разделе «Общая по чату → За сегодня» строка «💎 Добыто Пульсов» считала **только базовый майнинг** (`message_reward`), игнорируя комбо-награды, спринты, штрафы
▪️ Теперь отображаются две метрики:
  — **Добыто Пульсов** = message_reward + combo_reward + sprint_reward − штрафы
  — **Всего заработано** = + referral_reward, lottery_win, bingo_win, monthly_gift, реакции, лутбоксы, admin_give и т.д.
▪️ Файл: `handlers/Stats/stats_controller/callback.py`

### 🏆 ТОП АКТИВИСТОВ — ПРОГРЕВ КЕША ПРИ СТАРТЕ (V1.9.7d)
▪️ При запуске бота кеш «% активности» теперь **заполняется немедленно**, а не через час после старта
▪️ Ранее при перезапуске бота первый час показывалось «Пока нет данных об активности»
▪️ Механизм: 24-часовое скользящее окно по `user_stats_hourly`, обновление каждый час — остался прежним
▪️ Файл: `bot.py:post_init`

---

## 🔧 V1.9.6 — Журнал: форматирование всех событий по ТЗ (09.04.2026)

### 📓 ЖУРНАЛ — ЕДИНЫЙ ФОРМАТ ВСЕХ СОБЫТИЙ (V1.9.5d–V1.9.6)
▪️ Все события журнала (канал 3) переработаны по единому шаблону: **Группа → Инициатор → Пользователь → Время**
▪️ Новые хелперы: `_fmt_time_msk()`, `_fmt_chat_block()`, обновлён `_fmt_user_block()` — везде единый стиль
▪️ Изменения по событиям:
  — **#Вход** — только активные ID, без лишних скобок
  — **#Выход** — заголовок сокращён до «⚠️ #Выход»
  — **#Бан** — переименован из #Блокировка, добавлен блок триггера
  — **#Мут/#Размут/#Разбан** — Группа + Инициатор + Пользователь + Время
  — **#Триггер** — убран дублирующий хештег; добавлен блок «Сообщение пользователя» вместо верхнего #Триггер
  — **#Опрос** — все 5 полей анкеты + Группа + Пользователь + Время; убран дублирующий хештег
  — **#Профиль** — Было/Стало по каждому изменённому полю
  — **#Фото** — логирование смены/открытия/скрытия фото профиля
▪️ Файл: `handlers/journal_handlers.py`

### 🔗 ССЫЛКА НА СООБЩЕНИЕ В ЖУРНАЛЕ — ИСПРАВЛЕНИЕ (V1.9.6e)
▪️ В событиях #Триггер и #Бан ссылка на сообщение генерировалась как `t.me/c/{id}/{msg_id}/{msg_id}` (ID дублировался), и Telegram показывал «Сообщение удалено»
▪️ Причина: `thread_id = getattr(..., 'message_thread_id', None) or msg_id` — при отсутствии ветки подставлялся сам `msg_id`
▪️ Исправлено: если `thread_id` отсутствует или равен `msg_id` — генерируется короткий формат `t.me/c/{id}/{msg_id}`
▪️ Файл: `handlers/journal_handlers.py` (2 места: log_ban, log_trigger)

### 📷 ТРЕКИНГ ФОТО ПРОФИЛЯ — ИСПРАВЛЕНИЕ (V1.9.5d)
▪️ Логирование смены фото не срабатывало из-за раннего `return` при отсутствии данных пользователя в БД
▪️ Теперь трекинг фото работает независимо от наличия профильных данных
▪️ Добавлена инициализация «первого визита» — при первом сообщении юзера фото сохраняется без лога, последующие изменения логируются
▪️ Ошибки трекинга переведены с `logger.debug` на `logger.error(exc_info=True)` для видимости
▪️ Файл: `handlers/profile_tracker.py`

### 🎛 МЮТ ПО ID ИЗ ПАНЕЛИ ВЛАДЕЛЬЦА — РЕАЛЬНЫЙ ЧАТ (V1.9.6)
▪️ При муте пользователя по ID через Панель Владельца в журнале отображался «—» вместо названия группы
▪️ Исправлено: теперь бот получает реальный объект чата через `bot.get_chat()` перед логированием
▪️ Файл: `handlers/owner_handlers.py`

### 🎯 ТРИГГЕРЫ — ИСПРАВЛЕНИЕ ВЕРОЯТНОСТИ 0% (V1.9.6g)
▪️ Триггер с вероятностью 0% всегда срабатывал вместо того чтобы никогда не срабатывать
▪️ Причина: `t['probability'] or 100` — значение `0` считается falsy в Python → подставлялось 100
▪️ Исправлено: `t['probability'] if t['probability'] is not None else 100`
▪️ Файл: `handlers/triggers_handlers.py:_trigger_to_data`

### 🤖 ДЕТЕКЦИЯ ЛИЦА НА АВАТАРКЕ — УЛУЧШЕНИЕ (V1.9.6f)
▪️ `face_detector.py` использовал только один каскад `haarcascade_frontalface_default.xml` — не работал на повёрнутых, профильных или тёмных фото
▪️ Теперь последовательно проверяются 4 каскада: `frontalface_default`, `frontalface_alt`, `frontalface_alt2`, `profileface`
▪️ Добавлены 3 набора параметров от строгих к мягким (`minNeighbors`: 5 → 3)
▪️ Добавлен `cv2.equalizeHist` для выравнивания яркости
▪️ Файлы: `utils/face_detector.py`, `registration_system/utils/face_detector.py`

---

## 📓 V1.9.5 — Журнал: 3 канала, настройка через панель (09.04.2026)

### 📡 ЖУРНАЛ — ТРИ КАНАЛА (V1.9.5a–V1.9.5d)
▪️ Добавлена возможность подключить **до 3 Telegram-каналов/чатов** для журнала событий
▪️ Каждый канал настраивается независимо: ID чата и ID ветки (необязательно)
▪️ Поддерживается формат ввода `CHATID/THREADID` (например `-100123456/42`) при подключении
▪️ **Все события журнала** теперь маршрутизируются в **канал 3** (вместо раздельной маршрутизации по каналам)
▪️ Настройка через **Панель Владельца** → раздел «📓 Журнал»
▪️ Журнал доступен **замам** так же, как владельцу
▪️ Ввод ID работает и в ЛС бота, и в треде администраторов
▪️ Файлы: `handlers/journal_handlers.py`, `handlers/owner_handlers.py`, `handlers/message_handler.py`, `handlers/messages/admin_logic.py`

---

## 🚀 V1.9.3 — Статистика, ТОП и чистка панели (08.04.2026, вечер)

### 📊 БЛОК «ОТВЕТЫ» В ОБЩЕЙ СТАТИСТИКЕ — ПЕРЕРАБОТАН (V1.9.3a1)
▪️ Раньше показывались две строки «Ответы ↗» и «Ответы ↙» — но они **всегда были равны** по чату (математика закрытой системы: каждый отправленный ответ кем-то получен), что вводило в заблуждение
▪️ Теперь блок показывает 3 осмысленные метрики:
  — **Ответов всего** — общее число reply-сообщений
  — **Отвечали (уник.)** — сколько уникальных юзеров отвечали кому-то
  — **Получили ответ (уник.)** — сколько уникальных юзеров получали ответы
▪️ Файл: `handlers/Stats/stats_controller/callback.py`

### 🏆 ТОП-5 АКТИВИСТОВ — СКОЛЬЗЯЩИЕ СУТКИ (V1.9.3a2)
▪️ Окно расчёта процентов активности увеличено с **4 часов** до **24 часов** (скользящих суток)
▪️ Раньше топ дёргался каждые 4 часа, показывая только тех, кто был активен в последнее окно — теперь данные стабильнее и отражают реальную картину суток
▪️ Файл: `utils/exchange_rate.py:calculate_top5_percent` — `timedelta(hours=4)` → `timedelta(hours=24)`

### 🥈 ЗАМЫ — ПРЕСС-РЕЛИЗЫ ОКОНЧАТЕЛЬНО ЗАРАБОТАЛИ (V1.9.2c, hotfix)
▪️ Доработка V1.9.2b: при назначении зама в `db_friend.add_deputy` происходил `UPDATE users SET role=DEPUTY`, но **если у пользователя ещё не было строки** в таблице `users` (он не регистрировался во friend-боте), UPDATE затрагивал 0 строк → `is_deputy()` всегда возвращал False → пресс-релизы блокировались
▪️ Добавлен `INSERT OR IGNORE INTO users(tg_id, role) VALUES(?, 'deputy')` перед UPDATE — теперь назначение работает для любого юзера
▪️ Для уже назначенных замов сделан разовый SQL-фикс на сервере
▪️ Файл: `database/db_friend.py:add_deputy`

### 🎛 СТАРЫЙ «ПУЛЬТ ВЛАДЕЛЬЦА» — ОТКЛЮЧЁН (V1.9.3a3)
▪️ Раньше существовали **две конкурирующие панели**: новая «Панель Владельца» (`panel_main`) и старый «Пульт Владельца» (`owner_dashboard`). Из заявок и опросов выхода кнопка «Назад» вела на старый Пульт
▪️ Теперь все алиасы (`owner_dashboard`, `panel_main`) и все кнопки «Назад в Панель Владельца» ведут на **новую Панель**
▪️ Старый код `handlers/owner_handlers.py` остался в файле, но больше не вызывается из активных диспетчеров
▪️ Файлы: `handlers/callback/owner_callbacks.py`, `utils/callback_handler.py`, `handlers/admin_moderation.py`, `handlers/exit_survey_handlers.py`

---

## 🔧 V1.9.1–V1.9.2 — Большое обновление интерфейса и прав (08.04.2026)

### 🏅 ПРАВА ЗАМОВ — ПОЛНЫЙ ДОСТУП (V1.9.2b)
▪️ Замы теперь могут **публиковать Пресс-Релиз** — ранее бот молча игнорировал нажатия, теперь все функции создания/редактирования/публикации/планирования работают для замов так же, как для Владельца
▪️ Замы теперь могут **публиковать Гороскоп** — меню, предпросмотр и публикация теперь доступны
▪️ Все FSM-состояния ввода (расписание, текст, фото, время, параметры) теперь работают для замов в личном чате с ботом
▪️ Технически: проверки `user.id == admin_id` заменены на `_is_pr_privileged` / `_is_horoscope_privileged` → `is_deputy(user_id)` в `db_friend`

### 🎛 ПАНЕЛЬ ВЛАДЕЛЬЦА — РЕЖИМ ОДНОГО ОКНА (V1.9.2a)
▪️ Навигация по панели (кнопки «🔙 Назад», переходы между разделами) больше **не создаёт новые сообщения** — теперь редактирует существующее
▪️ Ранее каждое нажатие «Назад» отправляло новое сообщение с панелью, засоряя чат
▪️ Технически: `send_admin_panel()` заменён на `show_owner_dashboard()` (edit_message_text) в диспетчере owner_callbacks

### 🔍 ПРОВЕРКА НИКА — ИСПРАВЛЕНИЕ (V1.9.2a)
▪️ Устранён баг: бот сообщал «не найден ни в базе, ни в чате» для пользователей, которые есть в основной базе, но не прошли регистрацию через анкету
▪️ Добавлен фоллбэк: если пользователь не найден в БД анкетирования (`db_friend`) — поиск продолжается в основной базе (`bot_database.db`) по нику или Telegram ID
▪️ Результат отображается корректно с реальным именем и статусом в чате

### 📋 ДЕТАЛИЗАЦИЯ — ЛОКАЛИЗАЦИЯ ТИПОВ ТРАНЗАКЦИЙ (V1.9.2a)
▪️ В Excel-детализации устранены «английские коды» типов операций — добавлен перевод 13 ранее неизвестных типов:
  — `bbs_popularity` → «💫 Популярность BBS»
  — `reaction_given_reward` → «❤️ Реакция (поставлена)»
  — `reaction_received_reward` → «❤️ Реакция (получена)»
  — `donate_to_user` → «💝 Донат участнику»
  — `donate_to_bank` → «🏦 Донат в банк»
  — `bingo_win/ticket/refund/penalty` → «🎯 Бинго (…)»
  — `shop_purchase` → «🛒 Покупка в магазине»
  — `lootbox_win` → «📦 Лутбокс (выигрыш)»
  — `lottery_commission` → «🎰 Комиссия лотереи»
  — `compensation_reward` → «🔧 Компенсация»

### 💬 «ВСЕ ЗАЯВКИ ОБРАБОТАНЫ» — КНОПКА ВОЗВРАТА (V1.9.2a)
▪️ При обработке всех заявок в личном чате с ботом теперь показывается кнопка **«🔙 Назад в Панель Владельца»**
▪️ В треде чата (ветка регистрации) кнопка не показывается — логика не изменена

### 📖 FAQ — ОПИСАНИЕ ТОП-5 (V1.9.2a)
▪️ В разделе «Описание функций → ТОП-5» добавлено подробное описание:
  — Богачи: ранжирование по балансу Пульсов, способы его пополнить
  — Активисты: Индекс Активности за 7 дней, из чего складывается (сообщения, реакции, майнинг)
  — Указано, что администраторы и владелец в топы не входят
  — Слова-триггеры: `богач`, `богачи`, `активист`, `активисты`

---

### 🔄 РЕОРГАНИЗАЦИЯ ИНТЕРФЕЙСА (V1.9.1–V1.9.1b)

**💘 Шиппер:**
▪️ Тумблер вкл/выкл перенесён в **«Управление функциями»** (рядом с остальными фичами)
▪️ Настройки шиппера (меню, таймеры, фразы, мэтчи) перенесены в **[Меню] → 💘 Шиппер** — доступны прямо из главного меню
▪️ Убрана кнопка «💘 Шиппер» из нижней ReplyKeyboard

**⚙️ Панель Владельца → Система:**
▪️ «Управление функциями» перенесено из [Меню] в **[Панель Владельца] → [Система]**
▪️ «Скачать БД» перенесено из нижних кнопок панели в **[Панель Владельца] → [Система]**
▪️ В [Меню] «Управление функциями» больше не отображается

**🗂 Единообразие меню:**
▪️ Устранено расхождение между 5 реализациями главного меню — все приведены к единому виду
▪️ Устранено дублирование кнопки «Шиппер» (появлялась или не появлялась в зависимости от пути входа в меню)

---

### 🏆 ТОП-5 — ИСКЛЮЧЕНИЕ ПЕРСОНАЛА (V1.9.1a)
▪️ Администраторы, владелец и замы **исключены из ТОП-5 богачей и активистов** повсюду: в меню, по триггерам, через callback, в статистике
▪️ Кэш расширен до 10 записей (был 5) чтобы после фильтрации персонала оставалось 5 участников
▪️ Замы исключаются через асинхронный `get_all_deputies()` из `db_friend`

---

### 🃏 BBS — ПАКЕТ УЛУЧШЕНИЙ (V1.9.1)

**📤 Публикация:**
▪️ Добавлена кнопка **«📤 Опубликовать изменения»** в меню редактирования — теперь изменения явно применяются только по нажатию
▪️ Анкета обновляется «на месте» (`update_profile_in_place`) — без удаления и переотправки
▪️ Убрана подпись «© Pulse Chat Community 2026» из опубликованных анкет

**✏️ Редактирование:**
▪️ При редактировании поля теперь видно **текущее значение** («Текущее: Москва»)
▪️ Поле «О себе» стало **обязательным** — кнопка «Пропустить» убрана, при пустом вводе показывается ошибка

**🗑 Удаление:**
▪️ Исправлен баг: при анкете с несколькими фото удалялось только последнее сообщение (сами фото оставались в чате и задваивались при переопубликации)
▪️ Теперь в БД сохраняются ID **всех** сообщений медиа-группы + кнопка, все корректно удаляются

---

## 🔧 Обновления после V1.9.0 (04.04.2026)

**🏅 ЗАМ ВЛАДЕЛЬЦА (V1.9.0d–g):**
▪️ Добавлена роль «Зам Владельца» — назначается/снимается Владельцем по Telegram ID через раздел «👥 Админы» в панели
▪️ Зам видит полный функционал Владельца: кнопку «👑 Панель Владельца» в клавиатуре, все инструменты управления
▪️ Назначение синхронизируется в обе БД: `role='deputy'` (async) + `is_owner=1` (sync)
▪️ В списке персонала отображаются иконки: 👑 Владелец / 🥈 Зам / ⭐ Админ
▪️ Зам имеет доступ к: пресс-релизам, управлению функциями, гороскопу, банку, переводам, статистике, ТОП, лотерее (управление), бинго (управление), курсу валют (расширенный вид), детализации
▪️ Только основной Владелец может назначать/снимать замов

**🔥 ТОП-5 АКТИВИСТОВ — ИКОНКА (V1.9.0a):**
▪️ Заменена сломанная иконка на tg-emoji `❣️` с правильным emoji_id
▪️ Добавлен `parse_mode='HTML'` в трёх точках отображения ТОП-5: top_commands, stats_tops, messages/top_and_stats

**📊 СТАТИСТИКА — СРАВНЕНИЕ С ПРЕДЫДУЩИМ ПЕРИОДОМ (V1.9.0e):**
▪️ Добавлены дельта-показатели: 🔺 +X.X% / 🔻 -X.X% по каждому параметру (сравнение с аналогичным предыдущим периодом)
▪️ Убрано задвоение параметров «Сообщения» и «Активных участников»
▪️ Убрано форматирование с двумя нулями после запятой у целых чисел

**🤖 БОТ-СООБЩЕНИЯ В СТАТИСТИКУ НЕ СЧИТАЮТСЯ:**
▪️ Добавлена проверка `user.is_bot` при записи message_handler — сообщения от ботов не попадают в статистику

---

## 🔧 Обновления после V1.9.0 (03.04.2026)

**🚀 НОВЫЙ ФУНКЦИОНАЛ:**
▪️ В Pulse BBS добавлен раздел «Другое»: объявления по барахолке, аренде, услугам и помощи теперь создаются через отдельный пошаговый сценарий
▪️ Перед созданием объявления показываются правила безопасности с кнопками «Согласен / Не согласен» — без согласия анкета не откроется
▪️ Правила предупреждают о запрете номеров телефонов, цензуре фото/видео и модерации контента
▪️ Хештеги по категориям (#Аренда, #Продам_Куплю, #Услуги и др.) добавляются автоматически — видны прямо во время заполнения
▪️ Города выбираются из того же словаря, что и в основном BBS (пресеты + ручной ввод)
▪️ Поддержка до 4 медиафайлов в любой комбинации: 4 фото, 4 видео, 1 видео + 3 фото и т.д.
▪️ В панели Владельца добавлен отдельный тумблер «BBS: Другое» для быстрого включения и отключения раздела
▪️ В FAQ добавлено описание раздела «Другое»

**🎨 ВИЗУАЛ И УДОБСТВО:**
▪️ Перед публикацией — предпросмотр с кнопками «Опубликовать», «Редактировать» и «Отмена»
▪️ Кнопка «Редактировать» открывает меню точечного редактирования: можно поменять любое поле (категорию, город, имя, заголовок, описание, цену, медиа) без перезаполнения всей анкеты
▪️ Кнопка связи с автором и кнопка жалобы — по аналогии с основными BBS-анкетами
▪️ В меню BBS кнопка «📦 Другое» показывается только когда функция включена Владельцем

**🛠 ИСПРАВЛЕНИЯ:**
▪️ Исправлена ошибка запуска бота: функции клавиатур редактирования BBS перенесены в правильный модуль (ранее бот падал с `ImportError: _build_edit_params_keyboard`)
▪️ Убран несовместимый с текущей версией PTB параметр `message_effect_id` в BBS-callback

**⚙️ ПОД КАПОТОМ:**
▪️ Добавлена новая таблица `bbs_other_posts` для хранения объявлений раздела «Другое», их медиа, message_id и времени публикации
▪️ BBS-логика разделена на отдельные модули: callback, навигация, FSM ввода и FSM раздела «Другое»
▪️ Функции `_build_edit_params_keyboard`, `_build_edit_role_keyboard`, `_build_edit_city_keyboard`, `_build_edit_goal_keyboard` централизованы в `editing_bbs.py`

**🛡 БЛЭКЛИСТ — ПРОВЕРКА ПРИ /start:**
▪️ Если заблокированный пользователь нажимает /start — показывается уведомление о блокировке с причиной и ссылкой на администратора (ранее показывалось «не зарегистрирован»)

**📝 АНКЕТА — ЗАЩИТА ОТ НАЖАТИЙ КНОПОК:**
▪️ Кнопки reply-клавиатуры (Профиль, Курс, Меню, ТОП-5, Активности и др.) больше не записываются как ответы на вопросы анкеты
▪️ При случайном нажатии кнопки — сообщение удаляется, текущий шаг повторяется
▪️ Защита работает на всех 6 шагах: имя, возраст, дата рождения, город, терапия, реф. код

**📢 ЖУРНАЛ СОБЫТИЙ — ПОЛНАЯ ФУНКЦИОНАЛЬНОСТЬ:**
▪️ Подключены недостающие логгеры:
  — `log_mute` / `log_unmute` — при муте/размуте из чата (/mute, /unmute) и из панели владельца
  — `log_ban` — при бане из чата (/ban)
  — `log_exit_survey` — при завершении exit-опроса (причина ухода → журнал)
  — `log_admin_action` — при эмиссии пульсов и глобальном вайпе балансов
▪️ Ранее уже работали: log_join, log_leave, log_blacklist, log_trigger, log_profile_change

---

## 📦 V1.9.0 — Железобетонная проверка членства в чате (03.04.2026)

**🛠 ИСПРАВЛЕНИЯ (КРИТИЧНЫЕ):**
▪️ Централизованная функция `verify_chat_membership()` — единая точка проверки для всех хендлеров
▪️ Telegram API теперь единственный источник правды. БД-фоллбэк срабатывает ТОЛЬКО при сбое API (сеть, рейт-лимит), а НЕ при ответе «пользователь не в чате»
▪️ Закрыта дыра в `/start` (command_handler) — статус `approved/in_chat/registered/left` из БД больше не обходит проверку API
▪️ Закрыта дыра в `/register` (registration_conversation) — тот же обход через статус из БД друга
▪️ Закрыта дыра в ЛС-сообщениях (message_handler) — фоллбэк `is_left=0` больше не переопределяет ответ API
▪️ Закрыта дыра в BBS callback (callback_handler) — унифицирована проверка через общую функцию

**⚙️ ПОД КАПОТОМ:**
▪️ Новый модуль `utils/membership.py` — одна функция, 4 точки входа (start, register, private message, BBS callback)
▪️ Автосинхронизация БД: если API говорит «не в чате» → `is_left` ставится в 1, статус в БД друга меняется на `left`
▪️ Если API говорит «в чате» → `is_left` ставится в 0, статус обновляется до `in_chat`
▪️ Обработка `Forbidden: bot was blocked by the user` — бот больше не крашится при попытке ответить заблокировавшему пользователю
▪️ Адекватное сообщение для бывших участников: «Ты сейчас не состоишь в чате» вместо «Заявка потерялась»

---

## 📦 V1.8.9 — Восстановление веток + Архив НьюзON + Аудит (01.04.2026)

**🆘 СИСТЕМА ВОССТАНОВЛЕНИЯ ВЕТОК (Пульт Владельца):**
▪️ Новая кнопка «🆘 Восстановление веток» в главном меню Пульта Владельца
▪️ Восстановление BBS — перепубликация всех анкет из БД в ветку форума (thread 8)
▪️ Восстановление НьюзON — переотправка всех постов из архива в ветку форума (thread 26)
▪️ Меню подтверждения перед запуском с количеством записей
▪️ Защита от FloodControl: `asyncio.sleep(2)` между каждой публикацией
▪️ Итоговый счётчик: Успешно X / Ошибок Y
▪️ try/except на каждую итерацию — одна ошибка не прерывает весь процесс

**📰 АРХИВ ПРЕСС-РЕЛИЗОВ:**
▪️ Немедленная публикация через «🚀 Опубликовать сейчас» теперь сохраняется в `scheduled_posts` со статусом `published`
▪️ Публикация запланированного поста через edit-меню — статус меняется на `published` вместо удаления записи
▪️ Все пресс-релизы теперь остаются в БД → восстановление НьюзON работает для всех будущих публикаций

**🔍 АУДИТ АКТИВНОСТЕЙ:**
▪️ `gift_handlers.py` — имя победителя при `username = NULL` теперь отображает user_id вместо `@None`
▪️ `bingo_handlers.py` — ошибки обновления карточек в ЛС теперь логируются на уровне `warning` (были `debug`, в продакшне не были видны)

---

## 📦 Версия V1.8.8 (31.03.2026)

**🛠 АУДИТ РЕГИСТРАЦИИ — 7 ИСПРАВЛЕНИЙ:**
▪️ Проверка блокировки по возрасту (<18) при `/start` — заблокированный пользователь видит дату разблокировки и остаток дней, автоматическая разблокировка по достижении 18 лет.
▪️ Валидация длины имени, города и терапии (≥2 символа) — при вводе 1 символа бот просит повторить.
▪️ Валидация диапазона возраста (1–120) — бот отклоняет 0, 999 и прочие абсурдные значения.
▪️ Проверка будущей даты рождения — бот сообщает «Дата не может быть в будущем!».
▪️ Invite link: `member_limit=1` вместо `creates_join_request` — одобренный пользователь попадает в чат мгновенно, без повторной заявки.
▪️ Реализована кнопка «🗑 Удалить заявку» на карточке: статус DELETED, удаление карточки из треда, пользователь не уведомлён, заявка не возвращается в очередь.
▪️ Защита от двойного нажатия кнопок Одобрить/Отклонить/Удалить — повторное нажатие выдаёт alert «Заявка уже обработана».

---

## 📦 Версия V1.8.7 (31.03.2026)

**🛠 ИСПРАВЛЕНИЯ:**
▪️ После заполнения exit-опроса при выходе пользователя из чата бот теперь корректно отправляет отчет с ответами админу и владельцу (OWNER_ID, ADMIN_CHAT_ID) в ЛС.
▪️ Exit-опрос полностью исправлен: все ответы пользователя (включая пропущенные шаги) гарантированно сохраняются в одну запись.
▪️ Ссылка для возврата/входа — мгновенный вход если заявка одобрена, иначе join request.
▪️ Проверка статуса пользователя в чате через `get_chat_member` для мгновенного входа.

---

## 📦 Версия V1.8.6 (31.03.2026)

**🛠 ИСПРАВЛЕНИЯ:**
▪️ Если пользователь не найден в БД при одобрении заявки — бот автоматически подтягивает данные из Telegram API и создаёт запись (исчезли пустые карточки).
▪️ Убран параметр `message_effect_id` при отправке поздравления (несовместим с текущей версией PTB).

---

## 📦 Версия V1.8.5 (31.03.2026)

**🛠 ИСПРАВЛЕНИЯ:**
▪️ Восстановлена постоянная пользовательская клавиатура (ReplyKeyboard) для участников чата.
▪️ Поправлена логика отображения меню после прохождения регистрационных сценариев.

---

## 📦 Версия V1.8.4 (31.03.2026)

**🛠 ИСПРАВЛЕНИЯ СОВМЕСТИМОСТИ:**
▪️ Адаптация под PTB 21: импорт `ChatMemberStatus` переведён на актуальный путь `telegram.constants`.
▪️ Обновлены все места использования в обработчиках команд, сообщений, регистрации и callback-утилитах.

---

## 📦 Версия V1.8.3 (30.03.2026)

**🛠 ИСПРАВЛЕНИЯ ЭКОНОМИКИ И СТАТИСТИКИ:**
▪️ В `exchange_rate` убран лишний параметр divisor в TOP-5 снапшоте.
▪️ Исправлен знаменатель формулы курса и расчёт дельты изменения.
▪️ Скорректирован фильтр статистики — устранены искажения в отчётах.

---

## 📦 Версия V1.8.2 (30.03.2026)

**🛠 ИСПРАВЛЕНИЯ ПАНЕЛИ ВЛАДЕЛЬЦА:**
▪️ Панель владельца переведена в режим одного активного окна — старое сообщение редактируется вместо отправки нового.
▪️ Удалены дублирующиеся FSM-обработчики, вызывавшие повторные реакции интерфейса.

---

## 📦 Версия V1.8.1 (30.03.2026)

**🛠 ИСПРАВЛЕНИЯ ФОРМУЛЫ ТОП-5:**
▪️ Унифицирована формула АктП во всех модулях рейтинга TOP-5.
▪️ Синхронизированы расчёты в командах топа, модуле статистики и mining-хелперах — данные теперь идентичны во всех точках отображения.

---

## 📦 Версия RegMod-V1.8 (30.03.2026)

**🛠 РЕГИСТРАЦИЯ И МОДЕРАЦИЯ:**
▪️ Исправлен пакет из 10 ошибок в сценариях регистрации и модерации.
▪️ Стабилизирована работа шагов анкеты, переходов между состояниями FSM и проверки заявок администратором.

---

## 📦 Версия DB-V1.0 (30.03.2026)

**⚙️ ИНФРАСТРУКТУРА:**
▪️ Локальные файлы БД (`pulse_bot.db` и другие) исключены из git-репозитория.
▪️ В `.gitignore` добавлен паттерн `*.db` для предотвращения случайных коммитов рабочих баз данных.

---

## 📦 Версия V0.1 — Тестовый ИИ (29.03.2026)

**🚀 ЭКСПЕРИМЕНТАЛЬНЫЙ МОДУЛЬ:**
▪️ Подключён базовый контур тестового AI-ядра (`utils/ai_core.py`).
▪️ Добавлены команды-обработчики в командный слой и точка интеграции в основном боте.

---

## 📦 Версия V1.7.6b (29.03.2026)

**🛠 ИСПРАВЛЕНИЯ ДОСТУПА:**
▪️ Для `/start` и `/register` добавлен fallback через main DB при недоступности registration_system.
▪️ Восстановлен `reapply` callback для корректного повторного прохождения шагов регистрации.

---

## 📦 Версия V1.7.6a (29.03.2026)

**🛠 ИСПРАВЛЕНИЯ РЕГИСТРАЦИИ:**
▪️ Улучшен сценарий `/start` для действующих участников чата — прямой допуск без лишних шагов.
▪️ Закрыт кейс с зависшими заявками, которые блокировали повторный вход в поток модерации.

---

## 📦 Версия V1.7.6 (29.03.2026)

**🚀 НОВЫЙ ФУНКЦИОНАЛ:**
▪️ Реализован полный опросник при выходе из чата: 5 шагов, логика ветвления по причине, блокировка повторного опроса на 30 дней.
▪️ Внедрена автоматическая очистка сервисных сообщений бота в личке пользователя (кнопки «Вернуться», опросы).

**🛠 ИСПРАВЛЕНИЯ ДОСТУПА И МОДЕРАЦИИ:**
▪️ Полностью переработана логика `/start`: статус участника проверяется напрямую через Telegram API — бот автоматически обновляет БД и пускает к главному меню.
▪️ Зависшие заявки: старые анкеты автоматически закрываются при подтверждённом членстве.
▪️ Устранены критические ошибки панели администратора: `ValueError` при отмене, `NameError app_data`, цикличность анкет.
▪️ Исправлено распознавание статуса `RESTRICTED` — ограниченные участники корректно учитываются как члены чата.
▪️ Сообщения модерации корректно удаляются после принятия решения.

**🔐 БЕЗОПАСНОСТЬ И ССЫЛКИ:**
▪️ Все инвайт-ссылки переведёны на режим заявок (`creates_join_request=True`).
▪️ Автоодобрение сохранено только для доверенных пользователей (whitelist).

**⚙️ ПОД КАПОТОМ:**
▪️ Отключён автоматический постинг панели владельца в тред заявок при перезапуске сервера.

---

## 📦 Версия V1.7.5 (28.03.2026)

**🚀 НОВЫЙ ФУНКЦИОНАЛ:**
▪️ Реализован полный сценарий опроса при выходе из чата (п.6.3 ТЗ): 5 шагов с логикой ветвления и блокировкой повторного опроса на 30 дней.

---

## 📦 Версия V1.7.4 (28.03.2026)

**🏆 ТОП-5 — ЧИСТКА РЕЙТИНГОВ:**
▪️ Ушедшие из чата автоматически убираются из ТОП-5 при каждом просмотре — «живая» проверка через Telegram API.
▪️ При обнаружении ушедшего: баланс замораживается на 30 дней и возвращается в банк.
▪️ Добавлена команда `/remove_from_top @username` — ручное удаление из топов (доступно @Nersys и владельцу).

**📖 FAQ — ОПИСАНИЕ ФУНКЦИЙ:**
▪️ Раздел «Описание функций» показывает кнопки только для включённых функций.
▪️ Добавлены подробные описания 10 функций: профиль, топ, активности, детализация, лотерея, бинго, рефералы, донаты, подарок месяца, BBS.
▪️ Отключённые владельцем функции автоматически скрываются из списка FAQ.

**🛠 ИСПРАВЛЕНИЯ:**
▪️ Триггер «активист» в чате больше не вызывает ошибку (`CHARS_NORM not defined`).
▪️ В Excel-отчёт добавлен лист «СтатМайнинг».
▪️ В круговой диаграмме эмиссии убраны мусорные данные и нулевые доли.

---

## 📦 Версия V1.7.3 (28.03.2026)

**💰 ЭКОНОМИКА — ЗАМКНУТЫЙ КОНТУР:**
▪️ Общая эмиссия увеличена до 10 000 000 Пульсов.
▪️ Все операции переведены через Центробанк — экономический контур полностью замкнут.
▪️ Лотерея: покупка билетов → в банк, призы и рефанд → из банка.
▪️ Бинго: покупка карточек → в банк, призы / рефанд / штрафы → через банк.
▪️ Реактор: донаты возвращаются в банк (`reactor_balance` — метка).
▪️ Эмиссия владельца вычитает из банка (с проверкой остатка).
▪️ Monthly Gift берёт приз из банка (с проверкой остатка).
▪️ Вайп восстанавливает банк до 10 000 000.

**📊 ПАНЕЛЬ ВЛАДЕЛЬЦА:**
▪️ Добавлен live-дашборд экономики: баланс банка, сумма на руках у пользователей, заморожено.
▪️ Внедрена проверка инварианта: `bank + users + frozen = 10 000 000`.
▪️ Добавлена кнопка «Обновить» для мгновенного рефреша данных.

---

## 📦 Версия V1.7.2 (28.03.2026)

**🛠 ИСПРАВЛЕНИЯ ПРЕСС-РЕЛИЗОВ:**
▪️ Автоматический сплит текста пресс-релиза, превышающего 4096 символов.
▪️ Добавлен fallback-режим публикации без треда, если тред закрыт или не существует.
▪️ Исправлена ошибка `CAPTION_LIMIT not defined` в fallback-пути.
▪️ Команда `/panel` теперь отправляет панель в ту же ветку, откуда была вызвана.

---

## 📦 Версия V1.7.1 (28.03.2026)

**🚀 НОВЫЙ ФУНКЦИОНАЛ:**
▪️ Добавлена команда `/fix_left` для ручного исправления «зависших» статусов регистрации.
▪️ Подписи к медиа (caption) автоматически обрезаются до лимита 1024 символа.

**🛠 ИСПРАВЛЕНИЯ:**
▪️ Callback-кнопки регистрации (`reg_*`) добавлены в `entry_points` ConversationHandler — теперь работают корректно после перезапуска бота.

---

## 📦 Версия V1.7 (28.03.2026)

**🚀 ОНБОРДИНГ — ЕДИНОЕ ОКНО:**
▪️ Весь вступительный сценарий переработан: Приветствие → Подтверждение 18+ → Правила → Анкета — в одном сообщении без дублирования и лишних кликов.
▪️ Инвайт-ссылка скрыта в кнопке — защита от случайной пересылки ссылки в открытые чаты.

---

## 📦 Версия V1.6 (27.03.2026)

**🚀 НОВЫЙ ФУНКЦИОНАЛ — РЕГИСТРАЦИЯ И КОНТРОЛЬ ДОСТУПА:**
▪️ Реализована полная система регистрации: создание досье, контроль доступа, возможность перезапустить анкету.
▪️ Карточки заявок направляются в dedicated тред 241 для удобного просмотра модераторами.
▪️ Постоянная кнопка «Новые заявки» над панелью ввода в треде 241.
▪️ Панель владельца выводится как InlineKeyboard в треде заявок.
▪️ «Следующая заявка» редактирует существующее сообщение вместо отправки нового.

**🛠 ИСПРАВЛЕНИЯ:**
▪️ Устранён дубликат заявки при нажатии «Следующая заявка».
▪️ Защита от ошибки `Chat not found` при отправке заявки в чат администраторов.
▪️ `ADMIN_CHAT_ID` корректно задаётся с префиксом `-100` для Telegram API.
▪️ Исправлена обработка кнопок панели в отдельном чате администраторов.
▪️ Реализован корректный callback `reapply` для повторной подачи заявки без лишних шагов.

---

## 📦 Версия 26.03.2026

**🚀 НОВЫЙ ФУНКЦИОНАЛ:**
▪️ Запущен BBS-модуль (доска объявлений и знакомств): создание анкеты, просмотр других участников, общение в BBS-треде.
▪️ Реализован модуль статистики `handlers/Stats` — разбит на отдельные файлы по разделам.
▪️ Настроен автодеплой через GitHub Actions (публикация только из ветки `main`).
▪️ Исправлен абсолютный путь к БД и `.env` — бот корректно запускается из любой директории.
▪️ Добавлена защита от двойного запуска бота (PID-файл).

**🛠 ИСПРАВЛЕНИЯ:**
▪️ Удалены устаревшие системы, исправлен подсчёт майнинга в статистике.
▪️ Разрешены конфликты слияния в callback-роутере, обработчиках владельца, пользователей и статистики.
▪️ Исправлены ошибки в callback-роутере, owner/user callbacks, message handler и экспорте PDF.

**⚙️ ИНФРАСТРУКТУРА:**
▪️ Добавлены утилиты разработчика: `fix_user.py`, `resolve_conflicts.py`, черновик патчноутов `patchnote_draft.txt`.

---

## 📦 Версия 25.03.2026 — Слияние ботов

**🚀 НОВЫЙ ФУНКЦИОНАЛ:**
▪️ Завершена интеграция «малого бота» в основной проект — единая кодовая база.
▪️ Реализован шаг 6 ТЗ: анкетирование пользователей при выходе из чата (пп. 6, 6.1, 6.2).
▪️ Реализована полная система триггеров (шаг 7 ТЗ): автоматические реакции бота на ключевые события в чате.
▪️ Добавлена клавиатура опросника к системе триггеров.

**🎨 ВИЗУАЛ И УДОБСТВО:**
▪️ На панели владельца добавлены кнопки: Журнал, Статистика, Триггеры — прямо в главном меню.
▪️ Убран устаревший «Пульт Владельца», его функции перенесены в Панель.

**⚙️ РЕФАКТОРИНГ:**
▪️ Завершено слияние всех рабочих веток (main, dev, п.6 и п.7 ТЗ).
▪️ Файл `stats_handlers.py` разбит на отдельные модули по разделам.
▪️ Донастройка импортов и очистка файловой структуры от лишних папок.

---

## 📦 Версия 24.03.2026 — Регистрационная анкета

**🚀 НОВЫЙ ФУНКЦИОНАЛ:**
▪️ Реализован шаг 2 сценария регистрации.
▪️ Реализован шаг 3: логика согласия, отказа и пропуска анкеты.
▪️ Реализован шаг 4: полный функционал анкеты — одобрение, отклонение, кнопочные панели для Владельца и Администраторов.
▪️ Добавлена раздельная панель заявок: Владелец и Администраторы видят разные кнопки согласно своим правам.

---

## 📦 Версия 23.03.2026

**🚀 НОВЫЙ ФУНКЦИОНАЛ:**
▪️ Интеграция бота Вити в основной проект.
▪️ Добавлены новые активные товары в магазин.

**🎨 ВИЗУАЛ И УДОБСТВО:**
▪️ Исправлено отображение кнопки «Меню» (нижней) — не вызывалась из-за ошибки.
▪️ Обновлены тексты описаний разделов.

**⚙️ РЕФАКТОРИНГ:**
▪️ `stats_handlers.py` разбит на независимые модули — первый этап рефакторинга.
▪️ Настройка импортов после переноса файлов.
▪️ Очистка файловой структуры от папок и файлов не на своём месте.

---

## 📦 Версия 19–20.03.2026 — Базовая версия

**🚀 НОВЫЙ ФУНКЦИОНАЛ:**
▪️ Базовая рабочая версия бота с полноценной экономической системой.
▪️ Реактор 2.0 — обновлённая система донатов с новой механикой.
▪️ Бэкап базы данных для Владельца — выгрузка через меню.
▪️ Выходной контроль — передача особых данных при завершении сессии.
▪️ Созданы таблицы БД для расчёта майнинга.
▪️ Начало разработки сайта.

**🛠 ИСПРАВЛЕНИЯ:**
▪️ Починено отображение «Выгрузки БД» в меню Владельца.
▪️ Починена кнопка «Меню» внизу — не реагировала из-за ошибки обработчика.
▪️ Проведена очистка кодовой базы от лишних файлов.
